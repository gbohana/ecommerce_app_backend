<h1>Your cart</h1>
<ul>
    {{#each products}}
    <div id="list">
        <li>{{_id.title}} Description:{{_id.description}} Code:{{_id.code}}</br>
            Status:{{_id.status}} R${{_id.price}} In stock:{{_id.stock}} Categories:{{_id.categories}}</br>
            Quantity:{{quantity}}
        </li>
    </div>
    {{/each}}
    <button id="empty">Empty cart</button>
</ul>

<script>

</script>